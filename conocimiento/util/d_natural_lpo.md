L´ogica Intuicionista

Reglas B´asicas

$$
\frac { \Gamma \vdash \tau \lor \sigma \quad \Gamma , \tau \vdash \rho \quad \Gamma , \sigma \vdash \rho } { \Gamma \vdash \rho } \lor _ { e }
$$

Reglas Derivadas

$$
\frac { \Gamma \vdash \tau } { \Gamma \vdash \lnot \lnot \tau } \lnot \lnot \lnot _ { i }
$$

$$
\frac { \Gamma , \tau \vdash \sigma } { \Gamma \vdash \tau \Rightarrow \sigma } \Rightarrow _ { i }
$$

$$
\frac { \Gamma \vdash \tau \Rightarrow \sigma \quad \Gamma \vdash \tau } { \Gamma \vdash \sigma } \Rightarrow _ { e }
$$

$$
\frac { \Gamma , \tau \vdash \bot } { \Gamma \vdash \lnot \tau } \lnot _ { i }
$$

$$
\frac { \Gamma \vdash \tau \quad \Gamma \vdash \daleth \tau } { \Gamma \vdash \bot } \daleth _ { e }
$$

$$
\frac { \ \Gamma \vdash \tau \Rightarrow \sigma \quad \Gamma \vdash \neg \sigma } { \Gamma \vdash \neg \tau } M T
$$

Cuantificaci´on Universal

L´ogica Cl´asica

Regla B´asica

Reglas Derivadas

$$
\frac { \Gamma \vdash \lnot \lnot \tau } { \Gamma \vdash \tau } \lnot \lnot \lnot e
$$